# Local pack output + native payloads (natives are built/packed by CI, never committed)
artifacts/
**/runtimes/
bin/
obj/
TestResults/

# Vendored engine/source checkouts (built locally; never committed)
reference/

# Local test secrets (live-sync gates: TURSOSYNC_SYNC_SERVER, TWEED_TURSO_SYNC_*)
.env
