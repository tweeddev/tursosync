{
  "$schema": "https://raw.githubusercontent.com/JohnCampionJr/rig/main/node/rig.schema.json",
  "dotnet": {
    "solution": "TursoSync.slnx"
  },
  "commands": {
    "engine": {
      "description": "Pin the Turso engine in turso-engine.json (arg: a tag, or 'latest' for newest in series).",
      "command": "scripts/bump-turso.sh"
    },
    "pack": {
      "description": "Tier-0 local release test: build this host's native, pack, and consume-test from a local feed.",
      "command": "./local-pack.sh"
    }
  }
}
